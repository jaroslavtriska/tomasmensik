# Sanity CMS – project "TomasMensik" (org Jaroslav Triska)
# Get project ID from https://sanity.io/manage → that project → API
# Must match SANITY_STUDIO_PROJECT_ID so /admin and site use the same project
PUBLIC_SANITY_PROJECT_ID=your_tomasmensik_project_id
PUBLIC_SANITY_DATASET=production
